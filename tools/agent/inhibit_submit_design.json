{
  "name": "inhibit_submit_design",
  "description": "Run bloc screen on a design specification JSON file. Returns RELEASE, HOLD, or ESCALATE.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "required": ["spec_path"],
    "properties": {
      "spec_path": {
        "type": "string",
        "description": "Path to a design specification JSON file (spec_version 0.1.0)."
      }
    }
  }
}
